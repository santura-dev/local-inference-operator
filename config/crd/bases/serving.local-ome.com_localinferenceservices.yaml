---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: localinferenceservices.serving.local-ome.com
spec:
  group: serving.local-ome.com
  names:
    kind: LocalInferenceService
    listKind: LocalInferenceServiceList
    plural: localinferenceservices
    singular: localinferenceservice
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: LocalInferenceService is the Schema for the localinferenceservices
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of LocalInferenceService
            properties:
              model:
                description: Model defines the model configuration
                properties:
                  name:
                    description: Name is a human-readable name for the model
                    type: string
                  uri:
                    description: URI is the HuggingFace model URI (e.g., hf://facebook/opt-125m)
                    type: string
                  version:
                    description: Version specifies the model version
                    type: string
                required:
                - uri
                type: object
              runtime:
                description: Runtime specifies the inference runtime (e.g., "vllm"
                  or "sglang")
                enum:
                - vllm
                - sglang
                type: string
              scaling:
                description: Scaling defines replica and autoscaling settings
                properties:
                  autoScale:
                    description: AutoScale enables horizontal pod autoscaling
                    type: boolean
                  maxReplicas:
                    default: 10
                    description: MaxReplicas specifies the maximum number of replicas
                      for autoscaling
                    format: int32
                    minimum: 1
                    type: integer
                  minReplicas:
                    default: 1
                    description: MinReplicas specifies the minimum number of replicas
                      for autoscaling
                    format: int32
                    minimum: 1
                    type: integer
                  replicas:
                    default: 1
                    description: Replicas specifies the number of pod replicas
                    format: int32
                    minimum: 1
                    type: integer
                  targetCPU:
                    default: 70
                    description: TargetCPU specifies the target CPU utilization percentage
                      for autoscaling
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                type: object
              settings:
                description: Settings defines advanced inference settings
                properties:
                  batchSize:
                    default: 1
                    description: BatchSize specifies the batch size for inference
                    format: int32
                    minimum: 1
                    type: integer
                  gpuMemory:
                    description: GPUMemory specifies the GPU memory limit (e.g., "8Gi")
                    type: string
                  maxTokens:
                    default: 100
                    description: MaxTokens specifies the maximum number of tokens
                      to generate
                    format: int32
                    minimum: 1
                    type: integer
                  precision:
                    default: fp16
                    description: Precision specifies the numerical precision (e.g.,
                      "fp16", "fp32")
                    enum:
                    - fp16
                    - fp32
                    - int8
                    type: string
                type: object
            required:
            - model
            - runtime
            type: object
          status:
            description: status defines the observed state of LocalInferenceService
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the LocalInferenceService resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deploymentName:
                description: DeploymentName is the name of the created Deployment
                type: string
              phase:
                description: Phase represents the current phase of the LocalInferenceService
                type: string
              readyReplicas:
                description: ReadyReplicas indicates how many replicas are ready
                format: int32
                type: integer
              serviceName:
                description: ServiceName is the name of the created Service
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
